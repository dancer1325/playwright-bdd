# Debugging

* goal
  * debug -- , via [Playwright debugging methods](https://playwright.dev/docs/debug), -- BDD tests

## -- with -- `--debug` flag

* == `npx bddgen && npx playwright test --debug`
  * opens the browser
  * enable
    * step-by-step evaluation

![debug-flag](./_media/debug-flag.png)

## -- with -- `--ui` flag

* == `npx bddgen && npx playwright test --ui`
  * runs BDD tests | UI mode

[UI mode](guides/ui-mode.md)
